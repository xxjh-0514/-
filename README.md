# -
该项目是基于 C++ 面向对象开发的职工管理系统，核心采用抽象基类 + 子类多态设计，通过文件empFile.txt实现数据持久化。定义抽象类Worker，派生Employee、Manager、Boss子类并重写信息展示、岗位名称获取方法；WorkerManager类封装核心业务：初始化时检测文件状态，支持职工信息增删改查、按编号排序、清空文件，所有操作同步写入文件。程序入口实例化WorkerManager，通过菜单循环接收用户指令，调用对应功能，利用多态统一管理不同岗位职工，完成职工信息全生命周期管理。
